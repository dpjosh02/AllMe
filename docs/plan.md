# AllMe Linting Plan

This lowercase file is a compatibility pointer for tools or prompts that refer
to `docs/plan.md`.

Use `docs/PLANS.md` as the authoritative linting and static-analysis rollout
plan.

Current state:

- Phases 0, 1, 2, 3, and 4A-D are complete.
- Phase 5A is documentation sync only.
- Phase 5B should target `src/features/settings/queries.ts` and
  `src/features/today/queries.ts`.
